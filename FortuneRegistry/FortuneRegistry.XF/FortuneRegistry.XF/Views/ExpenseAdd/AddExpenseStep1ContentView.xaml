<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" x:Class="FortuneRegistry.XF.Views.ExpenseAdd.AddExpenseStep1ContentView">
    <StackLayout Padding="5" VerticalOptions="End" Orientation="Vertical">
        <Picker Title="Category" ItemsSource="{Binding DataContext.Categories}" SelectedItem="{Binding DataContext.Category}">
        </Picker>
        <Picker Title="Subcategory" ItemsSource="{Binding DataContext.Subcategories}" SelectedItem="{Binding DataContext.Subcategory}">
        </Picker>
    </StackLayout>
</ContentView>
